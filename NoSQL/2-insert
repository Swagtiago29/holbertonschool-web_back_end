//Insert the document into the 'school'
result = db.school.insertOne({
    name: "Holberton school"
});
// Mimic the WriteResult output format
print("WriteResult({ \"nInserted\" : " + (result.acknowledged ? 1 : 0) + " })");
